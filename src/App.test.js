import React from 'react';
import { shallow, mount } from 'enzyme';
import App from './App';

it.skip('renders App with className app', () => {
});

it.skip('initial state starts with an empty array', () => {
});

it.skip('having 2 thoughts in state should mean there are two thoughtCards', () => {
});

it.skip('user can add a new thought which adds it to state and is also displayed on the screen', () => {
});
